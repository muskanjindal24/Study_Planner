<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.studyplanner">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_studyplanner"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ResultActivity"></activity>

        <activity android:name=".Done"></activity>
        <activity android:name=".FinalData" />
        <activity android:name=".categoryActivity" />
        <activity android:name=".BooksList" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".Quiz" />
        <activity android:name=".material" />
        <activity android:name=".graph" />
        <activity android:name=".MainActivityHP" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>